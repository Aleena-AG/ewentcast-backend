# Deny Apache directory listing / direct file access if DocumentRoot is ever used.
Options -Indexes
<FilesMatch "^\.env">
  Require all denied
</FilesMatch>
<FilesMatch "\.(env|log|md|sql|ts|json)$">
  Require all denied
</FilesMatch>
